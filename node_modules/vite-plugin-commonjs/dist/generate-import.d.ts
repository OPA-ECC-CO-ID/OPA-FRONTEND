import type { ImportType } from 'src';
import type { Analyzed } from './analyze';
export interface ImportRecord {
    node: AcornNode;
    importExpression?: string;
    importedName?: string;
}
export declare function generateImport(analyzed: Analyzed, id: string, rules?: ImportType | ((id: string) => ImportType)): ImportRecord[];
